package ch06.Quiz;

public class MemberService {

	boolean login(String id, String password) {
		if("hong".equals(id)&&"12345".equals(passwords)) {
			return true;
		}
		return false;
		void logout() {
			
		};
		
	};
}
